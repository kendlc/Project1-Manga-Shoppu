<h1><%= @volume.title %></h1>

<%= link_to "Delete", @volume, :method => 'delete', :data => { :confirm => 'Are you sure'} %>

<%= form_for @volume do |f| %>
    <%= f.label :price%>
    <%= f.text_field :price %>
    
   
    <%= f.submit %>
<% end %>




